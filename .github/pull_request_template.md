## What changed

<!-- One paragraph. Reference the issue: "Closes #12". -->

## Evidence

<!-- The commands you ran and what they printed. Not "tests pass". -->

## Deviations

<!-- Anything you did differently from the issue, and why. "None" is a valid answer. -->

## Checklist

- [ ] Every commit carries `Signed-off-by` (`git commit -s`); there is no CLA.
- [ ] `make ci` is green locally.
- [ ] A bug fix ships with a committed reproduction artifact that fails without the fix.
- [ ] A new direct dependency ships with an ADR under `docs/adr/` and an entry in `scripts/dep-budget.sh`.
