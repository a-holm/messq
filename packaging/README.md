# packaging

Distribution artifacts live here: the systemd unit, `nfpm.yaml` for the `.deb` and `.rpm`, and the `Dockerfile`.

The directory is empty until issue #36 (packaging and release). The systemd unit is also shaped by issue #17 (daemon lifecycle and drain).
