# contrib

Operator-facing extras that ship with the repository but not inside the binary: `prometheus/alerts.yml` and `grafana/messq.json`.

The directory is empty until issue #21 (metrics, alert rules and dashboard).
