<!-- Generated by `make docs` from the command tree. Do not edit. -->

# messq_backup

See `messq help exit-codes` for the exit-code contract.

## messq backup

snapshot a data directory with VACUUM INTO

### Synopsis

Take a consistent, self-verifying snapshot of a messq data directory into a single file via SQLite's VACUUM INTO on a read-only connection.

The snapshot works while the daemon runs AND after it stops, is written to a private temp directory, and renamed into place only after its own self-check passes. Provenance is stamped into the file itself so it explains its origin to whoever restores it later.

```
messq backup <dest> [flags]
```

### Examples

```
  messq backup /var/backups/messq/$(date -u +%FT%H%MZ).db --data-dir /var/lib/messq # noexec: shell substitution and machine-specific paths
  messq backup /tmp/snap.db --data-dir /var/lib/messq --verify full --force # noexec: names machine-specific directories
```

### Options

```
      --data-dir string   source data directory holding messq.db
      --force             overwrite an existing destination (still temp+rename, never partial)
  -h, --help              help for backup
      --verify string     post-snapshot self-check: quick|full|none (default "quick")
```

### Options inherited from parent commands

```
  -a, --addr string         daemon address (unix://, http://, https://) (default "unix:///run/messq/messq.sock")
      --color string        colour: auto|always|never (default "auto")
      --full-ids            print full ULIDs instead of abbreviated ones
  -o, --output string       output mode: auto|table|json|ndjson (default "auto")
  -q, --quiet               suppress narration; data and errors unaffected
      --timeout duration    per-request deadline (0 = none) (default 30s)
      --token-file string   file holding the bearer token, mode 0600
  -v, --verbose count       narrate (-vv dumps each HTTP exchange and the resolved config)
```

### SEE ALSO

* [messq](messq.md)	 - a lightweight, single-binary queue daemon

###### Auto generated by spf13/cobra on 29-Aug-2026
