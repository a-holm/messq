# Binaries
/messq
/bin/
/dist/
*.exe
*.test

# Coverage
*.out
coverage.html

# Data / local state
*.db
*.db-wal
*.db-shm
/data/

# Editor/OS
.idea/
.vscode/
*.swp
.DS_Store

# Go workspace files, always local
go.work
go.work.sum

# rapid failfiles land here when a property fails. A genuine regression is committed by hand,
# with the fix; the ones a deliberately broken build produced are noise.
/internal/*/testdata/rapid/
